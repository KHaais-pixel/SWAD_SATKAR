# dependencies and build output, all regenerable
node_modules
node_modules.nosync
.next
.next.nosync
out
build

# scratch space this machine keeps outside iCloud's reach
*.nosync
!next-env.d.ts

# Vercel
.vercel

# test screenshots, regenerated by scripts/shots.mjs and friends
shots/

# the staff's own uploads and edits live on the server, not in the repo;
# the two JSON files are committed as the starting state
# guests' names and telephone numbers — the server's copy is the only one
content/reservations.json
content/uploads/*
!content/uploads/.gitkeep

# local
.DS_Store
._*
*.tmp
.env
.env.local
.vercel
